# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cogni-preview

resources:
  - ../../../base/node-app
  - env-state.yaml

namePrefix: resy-

images:
  - name: ghcr.io/cogni-dao/cogni-template
    newName: ghcr.io/cogni-dao/cogni-template
    digest: "sha256:26583f18fc780c23318ed436597bac8e0d1a9af3ad73dc56959a82da114d865b"
  - name: ghcr.io/cogni-dao/cogni-template-migrate
    newName: ghcr.io/cogni-dao/cogni-template
    digest: "sha256:e02979061618d06ce7cf41b36c32acc89e3d01a3b0578f1cea7d811bb943b32e"

patches:
  # Node-specific ConfigMap values.
  # DNS names must match prefixed Service names (namePrefix applies to all resources).
  - target:
      kind: ConfigMap
      name: node-app-config
    patch: |
      - op: replace
        path: /data/NODE_NAME
        value: "resy"
      - op: replace
        path: /data/TEMPORAL_NAMESPACE
        value: "cogni-preview"
      - op: replace
        path: /data/LITELLM_BASE_URL
        value: "http://resy-litellm-external:4000"
      - op: replace
        path: /data/TEMPORAL_ADDRESS
        value: "resy-temporal-external:7233"
      - op: replace
        path: /data/REDIS_URL
        value: "redis://resy-redis-external:6379"
      - op: add
        path: /data/NEXTAUTH_URL
        value: "https://resy-preview.cognidao.org"
  # NodePort + instance label on selector (prevents cross-node routing)
  - target:
      kind: Service
      name: node-app
    patch: |
      - op: add
        path: /spec/ports/0/nodePort
        value: 30300
      - op: replace
        path: /spec/ports/0/targetPort
        value: 3300
      - op: add
        path: /spec/selector/app.kubernetes.io~1instance
        value: "resy"
  # Fix secret refs + container port + instance label
  - target:
      kind: Deployment
      name: node-app
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/envFrom/1/secretRef/name
        value: "resy-node-app-secrets"
      - op: replace
        path: /spec/template/spec/containers/0/ports/0/containerPort
        value: 3300
      - op: add
        path: /spec/selector/matchLabels/app.kubernetes.io~1instance
        value: "resy"
      - op: add
        path: /spec/template/metadata/labels/app.kubernetes.io~1instance
        value: "resy"
  # Migration: shared migrator image runs operator's schema (all nodes share one schema).
  # Secret ref patched to resy's secret so DATABASE_URL points to cogni_resy.
  - target:
      kind: Job
      name: migrate-node-app
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/env/0/valueFrom/secretKeyRef/name
        value: "resy-node-app-secrets"
  - target:
      kind: EndpointSlice
      name: postgres-external-1
    patch: |
      - op: replace
        path: /metadata/labels/kubernetes.io~1service-name
        value: "resy-postgres-external"
  - target:
      kind: EndpointSlice
      name: temporal-external-1
    patch: |
      - op: replace
        path: /metadata/labels/kubernetes.io~1service-name
        value: "resy-temporal-external"
  - target:
      kind: EndpointSlice
      name: litellm-external-1
    patch: |
      - op: replace
        path: /metadata/labels/kubernetes.io~1service-name
        value: "resy-litellm-external"
  - target:
      kind: EndpointSlice
      name: redis-external-1
    patch: |
      - op: replace
        path: /metadata/labels/kubernetes.io~1service-name
        value: "resy-redis-external"
  - target:
      kind: EndpointSlice
      name: doltgres-external-1
    patch: |
      - op: replace
        path: /metadata/labels/kubernetes.io~1service-name
        value: "resy-doltgres-external"

replacements:
  - source:
      kind: ConfigMap
      name: env-state
      fieldPath: data.VM_IP
    targets:
      - select:
          kind: EndpointSlice
          name: postgres-external-1
        fieldPaths:
          - endpoints.0.addresses.0
      - select:
          kind: EndpointSlice
          name: temporal-external-1
        fieldPaths:
          - endpoints.0.addresses.0
      - select:
          kind: EndpointSlice
          name: litellm-external-1
        fieldPaths:
          - endpoints.0.addresses.0
      - select:
          kind: EndpointSlice
          name: redis-external-1
        fieldPaths:
          - endpoints.0.addresses.0
      - select:
          kind: EndpointSlice
          name: doltgres-external-1
        fieldPaths:
          - endpoints.0.addresses.0
